name: Event PR - Single Job222222

on:
  pull_request:
    branches:
      - 'main'

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world
        run: echo "Hello, world!"